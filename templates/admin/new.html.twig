{% extends 'base.html.twig' %}


{% block body %}
    <section id="section-post-new">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h3 class="h3 text-center">CREATION D'ARTICLE</h3>
                {{ form_start(newForm) }}
                {{ form_widget(newForm) }}
                {{ form_end(newForm) }}
            </div>
        </div>
    </div>
    </section>
{% endblock %}